#!/usr/bin/env ruby #The opening line is called a shebang because of the first two characters #!.
# frozen_string_literal: true

# This line provides the shell (in our case, bash) with the absolute path to the Ruby interpreter.

require 'bundler/setup' # require is a method that is used when you want to reference and execute code that is not written in your current file.
require 'ultra_races'

require 'irb'
IRB.start(__FILE__)
